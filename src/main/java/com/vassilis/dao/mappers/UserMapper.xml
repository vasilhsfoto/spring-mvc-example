<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.vassilis.dao.mappers.UserMapper">
	
  <select id="getEmployee" parameterType="string" resultType="com.vassilis.model.Employee">
    select * from employee where name = #{name}
  </select>
  
</mapper>